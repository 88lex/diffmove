set files require only two fields for diffsets to read
1. A configured rclone source remote
3. A configured rclone destination remote

#source_remote     destination remote    
my_source:         my_dest: